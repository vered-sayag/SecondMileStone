//
// Created by davidregev on 10/01/19.
//

#include "GraphSearchable.h"

GraphSearchable::GraphSearchable(vector < double > < vector > table) {
    State< double > tempState, init, goal;
    for (vector< vector< double > >::iterator matrixIt = table.begin(); matrixIt != table.end() ; ++matrixIt) {
        for (vector<double>::iterator rowIt = table.begin(); rowIt != table.end() ; ++rowIt) {

        }
    }
}